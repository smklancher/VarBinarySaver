# VarBinarySaver
Save SSMS VarBinary Results to Files - Quick utility to save arbitrary VARBINARY results to files.

Select or drag and drop a csv file.  For each row, the first column is used as a filename. Any columns with binary data (0x...) will be saved with that name plus an incrementing number in a subfolder of the location of the original csv file.
